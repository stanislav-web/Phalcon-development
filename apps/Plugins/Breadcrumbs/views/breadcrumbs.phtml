<div class="row">
    <div id="breadcrumb" class="col-xs-12">
        <ol class="breadcrumb">
            <?php foreach ($elements as $element): ?>
                <li>
                    <?= ($element['active']) ? $element['text'] : $this->tag->linkTo($element['link'], $element['text'], false); ?>
                </li>
            <?php endforeach; ?>
        </ol>
    </div>
</div>