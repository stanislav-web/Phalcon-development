paths:
    migrations: %%PHINX_CONFIG_DIR%%/build/migrations

environments:
    default_migration_table: phinxlog
    default_database: phalcon.local
    production:
        adapter: mysql
        host: localhost
        name: phalcon.local
        user: root
        pass: 'root'
        port: 3306
        charset: utf8

    development:
        adapter: mysql
        host: localhost
        name: phalcon.local
        user: root
        pass: 'root'
        port: 3306
        charset: utf8

    testing:
        adapter: mysql
        host: localhost
        name: phalcon.local
        user: root
        pass: 'root'
        port: 3306
        charset: utf8
